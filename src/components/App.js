import React, {Component, useState} from "react";
import '../styles/App.css';

const App = () => {
  return (
    <div id="main">
      <h1 data-ns-test="project-name">Digital Vehicle Fuel Level Indicator</h1>

<h2 data-ns-test="project-description">In our project the main blocks are micro controller unit, fuel level sensor and LCD display unit. The fuel level detection circuit is used to detect the level of the fuel in the tank; here sensors are placed at certain places to find out the fuel level and the signals are sent to the micro controller unit for further operations.</h2>

    </div>
  )
}


export default App;
